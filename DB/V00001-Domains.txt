CREATE DOMAIN "CODIGO-2" AS
CHAR(2) CHARACTER SET WIN1252
COLLATE WIN1252;

CREATE DOMAIN "CODIGO-20" AS
VARCHAR(20) CHARACTER SET WIN1252
COLLATE WIN1252;

CREATE DOMAIN "CODIGO-4" AS
VARCHAR(4) CHARACTER SET WIN1252
COLLATE WIN1252;

CREATE DOMAIN "DATA-HORA" AS
TIMESTAMP;

CREATE DOMAIN "DESC-150" AS
VARCHAR(150) CHARACTER SET WIN1252;

CREATE DOMAIN "DESC-50" AS
VARCHAR(50) CHARACTER SET WIN1252
COLLATE WIN1252;

CREATE DOMAIN QUANT AS
DECIMAL(9,2);

CREATE DOMAIN "VALOR-TOTAL" AS
DECIMAL(11,2);

CREATE DOMAIN "VALOR-UNIT" AS
DECIMAL(11,4);